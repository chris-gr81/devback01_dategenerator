# Node.js Demo Project

This is a small demo project for **Node.js**.

## Description

The program generates random timestamp entries within the **last 10 years**, sorts them, converts them into the **European date format**, and writes them to a text file that simulates a simple log.

## Usage

Start the program with:

```bash
npm run start <number>
```
